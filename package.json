{
  "name": "family-tutor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run check --prefix packages/core && npm run check --prefix skills/family-tutor/mcp-server && npm run check --prefix skills/family-tutor/runtime/tutor-orchestrator"
  }
}
